package net.fiercemanul.fiercesource.client.gui.style;

/**
 * @param leftOffset 画布左对tab左
 * @param topMargin 画布顶对tab的顶
 * @param bottomMargin 画布底对tab的底
 * @param width 宽度
 */
public record ScrollBarTabSize(
        int leftOffset,
        int topMargin,
        int bottomMargin,
        int width
) {
}
